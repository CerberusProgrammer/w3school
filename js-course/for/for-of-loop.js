var cars = ['BMW', 'Volvo', 'Mini'];
var x;

// para cada iteración, el valor de la siguiente propiedad se asigna a la variable x.

for (x of cars) {
    document.write(x + "<br >");
}

var txt = 'JavaScript';
var x;

for (x of txt) {
    document.write(x + "<br >");
}